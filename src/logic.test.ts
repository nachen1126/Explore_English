import {describe,expect,it} from 'vitest';import{matches,needsPractice,recommend,emptyProgress,type State}from'./logic';import{scenes}from'./data';
describe('learning logic',()=>{it('normalizes accepted answers',()=>expect(matches(" It's a jellyfish! ",['jellyfish',"it's a jellyfish"])).toBe(true));it('detects practice need',()=>expect(needsPractice({...emptyProgress(),findIncorrect:1})).toBe(true));it('recommends same topic and avoids current',()=>{const s:State={words:{},scenes:{}};const n=recommend('underwater',scenes,s,'underwater-1');expect(n.topicId).toBe('underwater');expect(n.id).toBe('underwater-2')})});
